apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: affine-cnpg-backup
  namespace: affine
spec:
  schedule: "0 3 * * *"          # 03:00 UTC daily — staggered 1h after authentik-cnpg
  backupOwnerReference: self
  method: volumeSnapshot
  cluster:
    name: affine-cnpg
