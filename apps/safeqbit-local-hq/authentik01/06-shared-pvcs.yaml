---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik01-media
  namespace: authentik01
  labels:
    app.kubernetes.io/name: authentik01
    app.kubernetes.io/component: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-truenas
  resources:
    requests:
      storage: 5Gi

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik01-templates
  namespace: authentik01
  labels:
    app.kubernetes.io/name: authentik01
    app.kubernetes.io/component: templates
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-truenas
  resources:
    requests:
      storage: 1Gi
